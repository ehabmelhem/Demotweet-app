import { ADD_TWEET } from "../actionTypes";
const initialState = {
  tweets: [],
};

const tweetReducers = ((state = initialState), action)=>{
    switch (action.type){
        caseADD_TWEET:{

        }
    }
}
